# Эксперимент H1: детерминированный граф сущностей (doc_graph)

Ветка: `hypo/h1-doc-graph` · Дата: 2026-09-03 · Код: `tz_review/passes/doc_graph.py`, флаг `use_graph` / вариант бенча `v1g`.

## Гипотеза

Обезличивание документов МТС (TABLE_*, FIELD_*, TOPIC_*…) — бесплатная разметка
сущностей: граф «где определено / где используется» строится regex-ами и ловит
ссылочный класс (C) и дрейф имён (D1) детерминированно, с precision ~1.0, без LLM.

Три проверки: (1) поля-фантомы — используются, но не описаны ни в одной структуре;
(2) дрейф имён таблиц — анаграммы '_'-токенов; (3) битые ссылки на разделы.

## Результат (bench, v1 → v1g)

| Цель | v1 | v1g | Дельта | Что добавилось |
|---|---|---|---|---|
| synth_v1 | 2/12 | **5/12** | +3 | C 2/2 (поле-фантом, битая ссылка), D 1/2 (дрейф имени) |
| doc3 (целевой) | 1/11 | **2/11** | +1 | **G3-2 (critical): FIELD_LAC/CELL_ID/TIME_STAMP/CELL_GEN/IMEI/IMSI — фантомы алгоритма** |
| doc2 | 0/6 | 0/6 | 0 | см. разбор ниже |
| doc1 | 1/8 | 1/8 | 0 | — |
| clean_base (шум) | 0 | **0** | 0 | ложных срабатываний нет |

Разрез по сложности на synth: hard 0/3 → **2/3** — граф ловит именно те дефекты,
которые для LLM «трудные» (связь двух мест документа).
Anchoring 100%, LLM-вызовов 0, время ~мгновенно.

## Где гипотеза ошибается / не достаёт

- **doc2 G2-2 не пойман**: FAQ ссылается на `radioAccessTechnology`, `CELL_ID`,
  `LAC` — имена БЕЗ префикса FIELD_ (стиль «сырых» имён источника). Regex-экстрактор
  видит только заглушки анонимизации. Расширение: словарь полей из таблиц структуры
  + матчинг голых имён (`cell_id` ≈ `FIELD_CELL_ID`) — кандидат в v2 гипотезы.
- **A-класс не трогает** (и не должен): отсутствие информации графом упоминаний
  не ловится — это территория чеклиста/GraphRLM-слотов.
- Дрейф ловится только при анаграмме токенов (TABLE_AGG_LOAD_LOG ↔
  TABLE_AGG_LOG_LOAD). Дрейф с заменой токена (СОТА↔ЯЧЕЙКА-стиль) — не ловится,
  нужно фаззи/эмбеддинги (доменный словарь синонимов).
- Определение поля = «первая/вторая ячейка строки таблицы» — эвристика; на
  документах без markdown-таблиц (плоский текст) деградирует до no-op.

## Вердикт

Гипотеза подтверждена: +3 дефекта на синтетике и +1 critical на целевом документе
при нуле ложных срабатываний и нулевой цене. Включаем в прод-вариант (v2g).
Следующая итерация: голые имена полей без префикса (закроет G2-2).
