# План на 06.09: точность 120b до 95 ± 1 % при полноте не ниже m9

Точка отсчёта (m9, прод-конфиг v2x + правила EXP-22 + стабильные батчи, ×2): precision **88 %**, шум@clean 7.5, doc3 15/16 ×2,
Σ 41.5/49; строгий v2g — 90 %, шум 5, doc3 14/16. Критерий протокола: 95 %. Правила чтения — PROTOCOL.md (п.5a обязателен).

Где сидит оставшийся мусор (слепая разметка m4/m7/m9): **общие «что если» взгляда разработчика** (≈ 60 % не-TP групп),
придирки к чистой базе (≈ 15 %), редкие FP чеклиста/уровня документа. По целям (m9): на реальном doc3 precision уже
96–100 % у обоих вариантов; провалы — на синтетике (v2hard 75–90 %, v3official 76–88 %, synth_v1 78–93 %).

## Рычаги по порядку (каждый — отдельная строка бенча ×2 и слепая разметка; ожидание пишется до прогона)
| # | Рычаг | Ожидание | Цена | Риск |
|---|---|---|---|---|
| 1 | **Промпт developer_sim v2**: каждый вопрос обязан (а) цитировать конкретное место, (б) назвать две конкретные реализации, между которыми выбор, (в) сказать, что именно изменится в данных; запрет общих формулировок «неправильный выбор может привести к…» | generic-whatif −50 %, потерь голда ≤ 2 (R18: dev_question держит трудные дефекты) | ~40 мин пода + разметка ~40 групп | полнота на doc3/v2hard |
| 2 | **Вторая разметка** (Виктор/Тимофей): пакет `eval/labels/m4_blind.yaml` без ключа, ≥ 40 групп, считаем согласие | precision сдвинется на ±3–5 п.п. в любую сторону — это измерение, не улучшение | 1 час человека | если эксперт считает «что если» законными — цель 95 % может оказаться уже достигнутой или недостижимой по определению |
| 3 | **Раздельный отчёт: реальные документы vs синтетика** | на doc1–3 после голда v2 для doc1/doc2 precision ≥ 90 % | офлайн | синтетика остаётся ниже — сказать честно |
| 4 | **Придирки к чистой базе**: разобрать 9 повторяющихся doc-level находок на clean (CHECK_* коды, FIELD_HOUR, ~350 строк, NO_REGION); настоящие пробелы → база it8 с записью в CHANGELOG, остальное → правило/промпт | шум@clean −2…−3 | 30 мин | подгонка базы под модель — только с записью «что и почему» |
| 5 | **Критик: инструкция специфичности** («снижай score вопросам без цитаты и без двух реализаций») | precision +2…+4 п.п. при потерях голда ≤ 1 | 25 мин пода | R13: score пока ничего не разделял |
| 6 | Если не хватает: **судья-проход** только для dev_question («вопрос обоснован документом и меняет реализацию?» → да/нет) | generic-whatif −70 %, +1 вызов на 5–8 вопросов | 40 мин пода | лишний вызов, латентность |

Метод: под поднимается один раз (`infra/runpod.py create-vllm-oss`, стрим включён), рычаги — последовательно, каждый против
m9 как базовой линии; пишем EXP-24 (developer_sim v2), EXP-25 (критик/судья) до запуска. Что не сработало — в REJECTED.

Проверено офлайн и отброшено сегодня утром: консенсус двух прогонов по тексту находки как фильтр — не метод (тексты одного и
того же вопроса от повтора к повтору разные; нужен семантический матч, отдельная задача).
