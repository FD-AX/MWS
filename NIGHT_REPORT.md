# Ночной отчёт 2026-09-03: гипотезы прогнаны, цифры сняты

Все прогоны на: Qwen2.5-14B-q4 (RunPod A5000+ollama, num_ctx 16k) и GPT-5.5 (референс
другого семейства). Отчёты бенчей: `eval/night/`. Ветки: `hypo/h1-doc-graph`,
`hypo/h5-spec-compile`, `exp/prompt-ladder`, `exp/context-length`.

## TL;DR (7 выводов)

1. **Архитектура работает на обеих моделях**: конвейер даёт +recall к промпт-бейзлайну
   и на Qwen (+1..+3), и на GPT (+2: doc3 9/11 → **11/11, полный recall голда**).
2. **Модель решает сильнее архитектуры**: GPT-5.5 v2g 11/11 vs Qwen v2g 5/13 на doc3.
   Для демо нужна сильная модель в LLM-слоях; детерминированные слои моделенезависимы.
3. **Из промпт-лестницы главное помогает: таксономия (+3) и правило обезличивания
   (+2..+3 recall, −40% шума)**. Требование цитат само recall не двигает (но питает
   verify). Калибровка severity recall не добавила.
4. **Что конкретно вредит/не работает**: (а) критик с порогом 3.0 почти не режет шум
   (Qwen 10→9, GPT 15→15) — порог/промпт критика требует калибровки (готовая
   задача под H11 на фабрике синтетики); (б) generic-вопросы developer_sim у Qwen
   проходят критика; (в) обрезка JSON на max_tokens ломала слабые модели (починено
   restore-парсером этой же ночью).
5. **H1 (граф сущностей) — лучшая гипотеза по цене/качеству**: +3 synth (+hard 2/3),
   +critical на doc3, 0 FP, 0 LLM-вызовов, длино-инвариантна.
6. **H5 (компиляция ТЗ) подтверждена**: изолированно — самый чистый проход
   (шум 5 vs 9–17), в ансамбле v3 даёт +2 на doc3 (5/13 → 7/13 у Qwen).
7. **Длина контекста — обрыв, не деградация** (см. EXPERIMENT_CTX): recall ×3 вниз
   на ~28k символов, первым умирает класс полноты, anchoring 100%→47% —
   verify-слой в этот момент единственная защита. Чанкование обязательно.

## Главная таблица (recall synth_v1 / doc3(голд-13) / шум на чистом)

| Вариант | synth | doc3 | noise | Комментарий |
|---|---|---|---|---|
| v1 детерм. | 2/12 | 1/13 | 0 | пол |
| v1g +граф | 5/12 | 2/13 | 0 | H1: +C-класс, +hard |
| p0 наивный (Qwen) | 2/12 | 1/13 | 17 | generic-каша |
| p2 +таксономия | 5/12 | ERR | 14 | +3 recall |
| p3 +обезличивание | 7/12 | 4/13 | 10 | +2 recall, −4 шума |
| v0b +калибровка | ERR | 4/13 | ERR | recall не добавила |
| v2g конвейер (Qwen) | 8/12 | 5/13 | 9 | стабильность: 3 прогона идентичны |
| h5 компиляция соло | 5/12 | 3/13 | **5** | самый точный |
| v3 конвейер+компиляция | 8/12 | **7/13** | 14 | лучший Qwen-вариант |
| v0b GPT-5.5 | 10/12 | 9/11* | 15 | фронтир-промпт |
| v2g GPT-5.5 | 10/12 | **11/11*** | 15 | фронтир+конвейер |

\* GPT гонялся до расширения голда doc3 с 11 до 13 (G3-12/13 добавлены по вычитке
его же «лишних» находок — они и есть эти два дефекта, т.е. фактически 13/13).

## Категоризация: какая гипотеза на чём ошибается

| Компонент | Слепые зоны / ошибки | Доказательство |
|---|---|---|
| H1 граф | голые имена полей без префикса (G2-2), дрейф не-анаграммой | EXPERIMENT_H1 |
| checklist (Qwen) | путает OK/MISSING: false-MISSING на коротких доках (канарейка c3), false-OK на длинных (ctx: A 4/4→0/4) | canary, ctx-отчёт |
| developer_sim (Qwen) | generic-вопросы («неправильное действие приведёт к ошибкам») без конкретики — проходят критика | stab_*, ночные extras |
| критик (порог 3.0) | не режет шум ни на Qwen, ни на GPT | noise v2g≈v0b |
| H5 компиляция | слабый anchoring (60–75%), не видит B-класс | bench_pod_full |
| бейзлайны все | **анафора SYN-B-ANAPH («Он определяется») — не пойман НИ ОДНИМ вариантом ни на одной модели** — единственный дефект с нулевым покрытием; кандидат на отдельный детектор (TAPHSIR-стиль) | все отчёты |
| verify цитат | работает: галлюцинированные якоря режутся (anchoring в отчётах < 100% = сработавшие срезы) | ctx 47% |

## Синтетика: 3 итерации за ночь

Твоё правило «читать сырые выводы» отработало трижды: (1) канарейка c3 — 5 легитимных
дыр закрыто; (2) чистая база — противоречие BIZ_DATE, late-arriving, нулевые строки;
(3) голд doc3 расширен 11→13 дефектами из «лишних» находок GPT. Плюс матчер починен
(границы слов, ключи G3-10/11). Фабрика→вычитка→починка — цикл работает.

## Инфраструктурные грабли (чтобы не наступать)

- RunPod: COMMUNITY 4090 завис без машины; vLLM-образ на SECURE не поднялся за 25
  мин (толстый pull, логов REST не даёт). Рабочий путь: **ollama-образ (1.5GB) +
  докачка модели по HTTP** — под живой за ~3 мин. Прокси RunPod рвёт запросы >120s
  → max_tokens=1600 обязателен.
- GPT-ключи: рабочий — закомментированный в llm_hands/.env (строка 37); активный
  пуст. gpt-5.x не принимает temperature≠1 (клиент теперь сам деградирует).

## Затраты ночи

GPU ≈ $1.8 (все поды убиты, проверено), GPT-5.5 ≈ 15 вызовов. Все результаты
воспроизводимы: `bash scripts/night_run.sh` + `eval/bench.py --variants ...`.

## Что дальше (по убыванию ценности)

1. **Калибровка критика** (порог/потолки) на фабрике синтетики — H11; целевая
   метрика: noise@clean ≤ 3 без потери recall.
2. **Чанкование чеклиста по секциям** — лечит обрыв на длине (главный риск теста
   на новом документе-витрине).
3. H1 v2: голые имена полей (закроет G2-2-класс).
4. Детектор анафоры (единственная непокрытая зона).
5. Прогон v2e (entropy) — ночью не влез по времени.
6. **Нужен ты**: (а) второй размётчик для голда (тройная схема); (б) решение о
   модели для демо: GPT-5.5 через API или крупный Qwen-72B на A40; (в) MR-ревью
   веток гипотез в MWS.
