# Направление: модели и 4 подхода (зафиксировано 2026-09-03)

## 1. Модели

Что показала ночь: качество LLM-слоёв решает сильнее архитектуры (GPT-5.5 v2g
11/11 vs Qwen2.5-14B v2g 5/13 на целевом doc3), детерминированные слои
моделенезависимы. Свежий ландшафт (сент. 2026): открытый топ — DeepSeek V4
(Flash/Pro, MIT), Qwen3.8 (Max / 27B dense), GLM-5.2, Kimi; Qwen3.x-27B dense
влезает в одну потребительскую карту при результатах уровня ~77% SWE-Bench
([Thunder Compute](https://www.thundercompute.com/blog/best-open-source-llms),
[wavect](https://wavect.io/blog/open-weight-llm-comparison-2026/),
[vellum](https://www.vellum.ai/open-llm-leaderboard)).

Роли (роутинг по этажам, CodeRabbit-стиль):

| Роль | Модель | Почему | Статус |
|---|---|---|---|
| Основные LLM-проходы + критик (демо) | **GPT-5.5** | 11/11 на doc3, ключ живой, проверен | ✅ подтверждено ночью |
| Дешёвые проходы (describe, entropy-сэмплы, канонизация) | **gpt-5.4-mini / nano** | есть на том же ключе | смоук не гонялся |
| Self-hosted трек (если жюри ценит локальность) | **Qwen3.8-27B** или **DeepSeek V4-Flash** на RunPod (ollama/vLLM, A40) | замена слабой 2.5-14B; 27B dense на одной карте | прогнать канарейку+бенч (~$1) |
| Референс лестницы | Qwen2.5-14B (ночные цифры) | зафиксирован как «дешёвый пол» | ✅ снято |

Правило выбора: любая смена модели = канарейка → бенч на targets_key → сравнение
строк. Никаких «на глаз».

## 2. Четыре подхода (остальное закрываем)

### A. Детерминированное ядро
Regex-слой + **H1-граф сущностей** (+ v2: голые имена полей, парсер таблиц H2,
ссылки H3, мини-детектор анафоры — единственная зона, где ночью промахнулись ВСЕ модели).
- Есть: 5/12 synth, 0 FP, 0 LLM-вызовов, длино-инвариантно.
- Метрика: recall C/F-классов → 1.0 при noise 0.
- Следующий шаг: голые имена (закрывает G2-2), «см. раздел N» в длинных доках.

### B. Слот-конвейер (чеклист → критик → verify)
Рубрика бинарных слотов + сравнительный критик + верификация цитат, **с чанкованием
по секциям** (лечим ночной обрыв на ~28k) и **калибровкой критика на фабрике
синтетики** (H11): ночью критик с порогом 3.0 шум не резал ни на одной модели.
- Есть: +2 recall к бейзлайну на GPT (9/11→11/11); стабильность 3/3.
- Метрика: noise@clean ≤3 без потери recall; recall длинных доков ≈ коротких.
- Следующий шаг: чанкование + hill-climb порога/потолков критика.

### C. Симуляция реализации (H5 → H6)
«Компиляция ТЗ» с журналом допущений; развитие — дифф двух независимых реализаций
(расхождения решений = неоднозначности логики).
- Есть: +2 doc3 в ансамбле, минимальный шум (5), уникальные находки вне рубрики.
- Метрика: recall классов A3/B9/E4 (краевые случаи, двойное прочтение) + шум ≤5.
- Следующий шаг: строже цитаты (anchoring 60→90%+), реализация H6-диффа.

### D. GraphRLM-полнота + semantic entropy (твой differentiator)
Слот-граф из GraphRLM как движок полноты (coverage-число) + энтропия ответов как
мера неоднозначности (порог калибруем бэктестом по голду).
- Есть: скаффолд entropy-прохода (`--entropy`, ночью не гонялся), GraphRLM-репо,
  MiniLM-энкодер для кластеризации.
- Метрика: coverage корректно ранжирует doc1-3 и синтетику; entropy AUC против
  B-класса голда; дельта recall к варианту B.
- Следующий шаг: прогон v2e на GPT; порт слот-графа из GraphRLM.

### Закрываем/поглощаем
Промпт-лестница p0–p3 — сыграла (выводы в EXPERIMENT_PROMPTLADDER), остаётся
референсом v0b. developer_sim — поглощается C (компиляция строже). Отдельная
энтропия — внутри D. Ансамбль подходов A+B+C+D = прод-вариант (сейчас v3+),
меряется той же таблицей бенча.
