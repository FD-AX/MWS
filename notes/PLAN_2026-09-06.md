# План на 06.09: полнота 120b до ≥ 98 % (не ниже), точность — не хуже сегодняшней

Решение 05.09 10:45 (Леонид): приоритет — **recall ≥ 98 %** по голду; precision держим не ниже m9 (88 % у v2x), не оптимизируем.
Точка отсчёта (m9, v2x + правила EXP-22 + стабильные батчи): Σ 44/49 медиана (90 %), объединение двух повторов 44/49.
98 % на 49 дефектах = **не больше одного промаха**. Правила чтения — PROTOCOL.md.

## Что мешает (по 26 прогонам ночи, доля прогонов без попадания)
| Дефект | промахов | класс | почему модель не берёт | рычаг |
|---|---|---|---|---|
| SYN-B-ANAPH | 96 % | B3 анафора («по нему» без антецедента) | ни один LLM-проход 120b не видит местоимение без референта | **детерминированный детектор анафоры**: местоимение/«это поле» в предложении без FIELD_*/TABLE_* и без сущности в предыдущем предложении → UNCLEAR |
| SYN-E-MEAS | 96 % | E1 неизмеримая формулировка допуска | «в пределах допустимого» читается как норма | **языковые паттерны** рубрики: «незначительн», «в пределах допустим», «приемлем», «существенно не», «без заметных» рядом с проверкой/сверкой/порогом → high |
| HRD-RET | 88 % | D2 глубина сверки 36 мес. при retention 24 | сверка величин между разделами (EXP-20 показал нестабильность LLM-прохода) | **программная сверка величин** (H15): регуляркой собрать «N месяцев/дней» с контекстом (хранен/retention vs сверк/реконсиляц/перезагруз/глубин) → пары «операция глубже хранения» → finding без LLM |
| HRD-KEY | 69 % | D3 из ключа убрано поле группировки | нужно сравнить два списка | **детерминированно**: поля из «ключ/PK/уникальн» vs поля из «группировк(а|и) по» — разность → finding |
| SYN-A-BCKF / G3-7 | 62 % / 54 % | A бэкфилл/глубина отсутствуют | слот HIS-01 применим только когда история упомянута — а дефект в том, что её **нет** | HIS-01: применимость для любой витрины/агрегата (`витрин|агрегат|cdm`), вопрос «задана ли глубина данных и порядок исторической загрузки» |
| OFF-SER | 46 % | A1 «формат по умолчанию платформы» | модель считает «по умолчанию» указанием | правка вопроса SER-01: «формат должен быть назван явно; „по умолчанию платформы“/„стандартный“ = не указан» + языковой паттерн |
| G3-9, HRD-BOUND, G3-8, OFF-EXAMPLE, HRD-FILT, OFF-REF | 19–31 % | разброс | ловятся в большинстве прогонов | зонд (v2s давал v3official 13/13 ×2) поверх v2x → строка **v2xs**; при необходимости консенсус-объединение двух прогонов (+1…+2, дёшево) |

Объединение двух повторов само по себе даёт +0…+2 — не путь. Путь — закрыть 5 системных промахов детерминированно
(они же самые «дешёвые» по точности: правила по построению не шумят), потом добрать разброс зондом.

## Порядок работ (каждый шаг — EXP с ожиданием до прогона; сначала офлайн на сохранённых документах, потом под)
1. **EXP-24 детерминированный слой v2** (без пода, офлайн-проверка на всех 7 документах + clean на ложные срабатывания):
   анафора, неизмеримые формулировки, сверка величин, ключ vs группировка. Ожидание: SYN-B-ANAPH, SYN-E-MEAS, HRD-RET, HRD-KEY
   закрыты в 100 % прогонов; ложных на clean/doc1/doc2 — 0…1.
2. **EXP-25 слоты**: HIS-01 применимость для витрин, SER-01 формулировка. Ожидание: SYN-A-BCKF/G3-7/OFF-SER закрыты ≥ 90 %.
3. **EXP-26 v2xs** (v2x + зонд Монте-Карло) ×2 на targets_key + docs: ожидание Σ ≥ 48/49, шум ≤ 12, precision ≥ 85 %.
4. Если остаётся 1–2 промаха — консенсус-объединение двух прогонов как режим «максимальная полнота» (цена ×2 времени).
5. Recall «с поправкой на матчер»: для каждого оставшегося промаха ручная проверка покрывающей находки.

Честные оговорки: детекторы п.1–2 калибруются на наших инъекциях — это классы дефектов (анафора, неизмеримость, глубина vs
хранение, ключ vs группировка), а не строки; проверка переносимости — doc1/doc2 и чистая база (ложные срабатывания).
Под поднимается один раз под п.3–4 (~1.5 ч, ≈ $5).

Вчерашний план по точности (95 ± 1 %) отложен решением; его рычаги — в истории файла (коммит 6601d51).
